[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "antonino_tuttofare_pi3bplus"
version = "0.1.0"
dependencies = [
    "psutil",
    "requests",
    "flask"
]

[tool.setuptools.packages.find]
where = ["."]